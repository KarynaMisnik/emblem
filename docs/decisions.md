# decisions

Some elements are flagged with a message:
Cannot use JSX unless the '--jsx' flag is provided.

Solution:
Run inside a project folder:
<code>npx expo install</code>

Then:
<code>npx expo start</code>

Reopen Visual Studio Code.
